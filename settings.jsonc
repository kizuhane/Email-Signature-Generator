{
  // File extension of output files
  "OutputFileExtension":"html",

  // Save All output in one file
  "SaveAllInOneFile":"true",

  //Name of file when saved in multiple file use template string formula with names from database to create unique names
  "OutputFileName":"${this.name}-${this.surname}--${this.job_position}"
}
